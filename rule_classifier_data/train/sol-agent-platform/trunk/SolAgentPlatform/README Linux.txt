Es posible que se tenga que ejecutar en el terminal la siguiente orden antes de la primera instalación en una máquina:

- Ejecutar en un terminal la orden: sudo ln -s /usr/lib/libbluetooth.so.3.5.0 /usr/lib/libbluetooth.so
